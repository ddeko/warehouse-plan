# Apps Script web app that appends feedback to the spreadsheet.
# Deploy apps-script/feedback.gs and paste its /exec URL here.
# Vite inlines VITE_* at build time, so this must be set in Vercel's project
# environment variables before the build, not after.
# Leave it unset and the feedback button simply does not render.
VITE_FEEDBACK_URL=
